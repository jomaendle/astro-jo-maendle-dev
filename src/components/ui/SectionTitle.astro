---
interface Props {
  showDivider?: boolean;
}

const { showDivider } = Astro.props;
---

<h1 class="text-2xl text-primary z-20">
  <slot />

  {showDivider && <div class="w-full h-[1px] bg-secondary mt-2" />}
</h1>
