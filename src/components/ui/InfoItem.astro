---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="flex flex-col gap-2">
  <p class="text-2xl text-secondary">{title}</p>
  {subtitle && <p>{subtitle}</p>}
  <slot />
</div>
